@{
    ViewData["Title"] = "Sign Up";
}

<h2 class="brand-mark mb-4">Sign up as…</h2>
<div class="d-flex gap-3">
    <a class="btn-auth-signup" asp-action="RegisterStudent">Student</a>
    <a class="btn-auth-signup" asp-action="RegisterTeacher">Teacher</a>
</div>
